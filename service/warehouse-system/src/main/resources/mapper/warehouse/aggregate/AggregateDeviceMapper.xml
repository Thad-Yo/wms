<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoyai.warehouse.mapper.AggregateDeviceMapper">
    <resultMap type="AggregateDevice" id="AggregateDeviceResult">
        <result property="deviceId" column="device_id"/>
        <result property="deviceCode" column="device_code"/>
        <result property="deviceName" column="device_name"/>
        <result property="deviceType" column="device_type"/>
        <result property="locationName" column="location_name"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectAggregateDeviceList" parameterType="AggregateDevice" resultMap="AggregateDeviceResult">
        select device_id, device_code, device_name, device_type, location_name, warehouse_id,
               warehouse_name, status, del_flag, create_by, create_time, update_by, update_time, remark
        from aggregate_device
        <where>
            and del_flag = '0'
            <if test="deviceCode != null and deviceCode != ''">and device_code like concat('%', #{deviceCode}, '%')</if>
            <if test="deviceName != null and deviceName != ''">and device_name like concat('%', #{deviceName}, '%')</if>
            <if test="deviceType != null and deviceType != ''">and device_type = #{deviceType}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by device_id desc
    </select>
</mapper>
